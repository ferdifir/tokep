# Choose the template that matches the environment:
#
# - Local development: copy `.env.development.example` to `.env`
# - Production server: copy `.env.production.example` to
#   `/var/www/tokep/shared/.env.production`
#
# Never commit real `.env` files.
